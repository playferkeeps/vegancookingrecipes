import { Recipe } from '@/types/recipe';
import { generateRecipe } from './recipeGenerator';

// Reset counter for this file
// Note: In production, IDs should be managed centrally

export const bakingRecipes: Recipe[] = [
  // Existing recipes will be migrated here
  // Then we'll add many more using the generator
];

