/**
 * Existing 8 recipes migrated to new structure
 * These preserve the original recipe IDs (1-8)
 * 
 * For now, this is empty. The original 8 recipes are preserved in recipes.ts
 * and will be included via the allRecipes export in index.ts
 */

import { Recipe } from '@/types/recipe';

// Empty for now - original recipes are in recipes.ts and included via index
export const existingRecipes: Recipe[] = [];

